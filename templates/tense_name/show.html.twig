{% extends 'base.html.twig' %}

{% block title %}TenseName{% endblock %}

{% block body %}
    <h1>TenseName</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tense_name.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tense_name.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tense_name_index') }}">back to list</a>

    <a href="{{ path('tense_name_edit', {'id': tense_name.id}) }}">edit</a>

    {{ include('tense_name/_delete_form.html.twig') }}
{% endblock %}
